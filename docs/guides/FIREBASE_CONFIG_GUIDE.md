# Firebase 配置取得指南

## 🔥 如何取得 Firebase Web 配置

您目前遇到登入錯誤是因為 Firebase 配置尚未完成。請按照以下步驟操作：

### 方法 1：從 Firebase Console 取得配置（推薦）

1. **前往 Firebase Console**
   - 開啟 https://console.firebase.google.com/
   - 使用您的 Google 帳號登入

2. **選擇專案**
   - 點選 `petshop-6d16a` 專案

3. **取得 Web 應用程式配置**
   - 點擊左側齒輪圖示 → 「專案設定」
   - 滾動到「您的應用程式」區域
   - 如果沒有 Web 應用程式：
     - 點擊 `</>` (Web) 圖示
     - 輸入應用程式暱稱（例如：Pet Shop Web）
     - **不要**勾選 Firebase Hosting
     - 點擊「註冊應用程式」
   - 如果已有 Web 應用程式：
     - 點擊現有的 Web 應用程式
   - 複製 `firebaseConfig` 物件中的所有值

4. **更新配置文件**
   - 開啟 `src/libs/firebase.ts`
   - 將複製的值貼上替換現有配置

### 方法 2：啟用 Authentication

在取得配置後，還需要：

1. **啟用 Email/Password 登入**
   - Firebase Console → Authentication → Sign-in method
   - 點擊「開始使用」（如果是第一次）
   - 啟用「電子郵件/密碼」
   - 點擊儲存

2. **啟用 Google 登入**
   - 同樣在 Sign-in method 頁面
   - 啟用「Google」
   - 設定專案的公開名稱
   - 選擇專案支援電子郵件
   - 點擊儲存

3. **檢查授權網域**
   - Authentication → Settings → Authorized domains
   - 確認 `localhost` 在清單中

### 方法 3：創建測試用戶

配置完成後，創建測試帳號：

1. Firebase Console → Authentication → Users
2. 點擊「Add user」
3. 輸入：
   - Email: test@petshop.com
   - Password: test123456
4. 點擊「Add user」

## 📝 配置範例

您的 `firebaseConfig` 應該看起來像這樣（這是範例，請使用您自己的值）：

```typescript
const firebaseConfig = {
  apiKey: "AIzaSyC...(您的真實 API Key)",
  authDomain: "petshop-6d16a.firebaseapp.com",
  projectId: "petshop-6d16a",
  storageBucket: "petshop-6d16a.firebasestorage.app",
  messagingSenderId: "114454743443166440250",
  appId: "1:114454743443166440250:web:a1b2c3d4e5f6...(您的真實 App ID)"
};
```

## ⚠️ 常見錯誤

### "登入失敗，請稍後再試"
- **原因**: Firebase 配置未完成或 Authentication 未啟用
- **解決**: 完成上述步驟 1-3

### "auth/invalid-api-key"
- **原因**: API Key 不正確
- **解決**: 重新從 Firebase Console 複製正確的 API Key

### "auth/operation-not-allowed"
- **原因**: 登入方式未在 Firebase Console 啟用
- **解決**: 啟用 Email/Password 和 Google 登入方式

### Google 登入失敗
- **原因**: Google 登入未配置或授權網域未設定
- **解決**: 確認 Google 登入已啟用且 localhost 在授權網域中

## 🚀 完成後測試

1. 儲存 `src/libs/firebase.ts` 文件
2. 瀏覽器會自動重新載入
3. 點擊導航欄的 👤 登入按鈕
4. 使用創建的測試帳號登入
5. 或點擊「使用 Google 登入」

## 需要幫助？

如果仍然遇到問題，請檢查：
- 瀏覽器控制台的錯誤訊息（按 F12）
- Firebase Console 中的配置是否正確
- Authentication 功能是否已啟用
